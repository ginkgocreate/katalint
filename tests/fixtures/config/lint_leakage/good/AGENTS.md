# Frontend Agent Guide

Formatting and linting are enforced by the project tooling; do not restate
individual rules here. See `.prettierrc` and `.eslintrc` for the authoritative
style configuration, and run the linter before committing.

When editing frontend files, keep changes focused and ask questions if the
requested behavior is unclear.
